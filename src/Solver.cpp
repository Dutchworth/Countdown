#include "Solver.h"
#include "Dictionary.h"
#include <string>

bool Solver::findWord(Dictionary & map,
                      std::string  letters,
                      std::string& foundWord) {
  // TODO
}
